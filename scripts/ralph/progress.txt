# Ralph Progress Log
Started: Thu Mar 12 02:17:17 AM +07 2026
---

## 2026-03-12 - US-003
- Added missing fields to `Simulation` model: fork_sim_code, start_date, curr_time, sec_per_step, maze_name, step; made `name` unique
- Created `Persona` model with all required fields (simulation FK, name, first_name, last_name, age, innate, learned, currently, lifestyle, living_area, daily_plan_req, status) with UNIQUE constraint on (simulation_id, name)
- Updated `admin.py` to register `Persona` with list_display and search_fields
- Created migration `0007_simulation_extra_fields_persona.py` manually (couldn't run makemigrations due to missing numpy in dev env)
- Files changed: `frontend_server/translator/models.py`, `frontend_server/translator/admin.py`, `frontend_server/translator/migrations/0007_simulation_extra_fields_persona.py`
- **Learnings for future iterations:**
  - `makemigrations` requires full app import chain (numpy, psycopg2, etc.) — create migration files manually when env is incomplete
  - Migration 0010 references 0009 (which doesn't exist yet) — this will be resolved when US-007 creates migration 0009
  - ESLint pre-commit hook only triggers on `frontend/src/**` files — Python-only commits skip it safely
  - The existing `Agent` model (from 0006) is kept alongside the new `Persona` model; they serve different purposes in the migration path
---

## 2026-03-12 - US-004
- Created `PersonaScratch` model with one-to-one FK to `Persona` and all 40+ fields
- Perception: vision_r, att_bandwidth, retention
- Temporal: curr_time, curr_tile (JSONB), concept_forget, daily_reflection_time, daily_reflection_size
- Scoring: overlap_reflect_th, kw_strg_event_reflect_th, kw_strg_thought_reflect_th, recency_w, relevance_w, importance_w, recency_decay, importance_trigger_max, importance_trigger_curr, importance_ele_n, thought_count
- Schedule: daily_req (JSONB), f_daily_schedule (JSONB), f_daily_schedule_hourly_org (JSONB)
- Action: act_address, act_start_time, act_duration, act_description, act_pronunciatio, act_event (JSONB), act_obj_description, act_obj_pronunciatio, act_obj_event (JSONB)
- Chat: chatting_with, chat (JSONB), chatting_with_buffer (JSONB), chatting_end_time, act_path_set, planned_path (JSONB)
- Created migration `0008_personascratch.py`
- Registered in admin with list_display and search_fields
- Files changed: `frontend_server/translator/models.py`, `frontend_server/translator/admin.py`, `frontend_server/translator/migrations/0008_personascratch.py`
- **Learnings for future iterations:**
  - Migration 0010 (already exists) depends on 0009_demo_conversation_tables — US-005/006/007 must create migrations 0008 and 0009 in the correct order
  - Migration 0010 expects: conceptnode, demo, demomovement, environmentstate, keywordstrength, movementrecord, personascratch, runtimestate, spatialmemory — all must exist by migration 0009
  - US-004 PersonaScratch → 0008; US-005+006+007 models → need to fit into 0009
---

## 2026-03-12 - US-005
- Created `EnvironmentState` model with simulation FK, step INT, agent_positions JSONB; UNIQUE(simulation, step); indexed on (simulation, step)
- Created `MovementRecord` model with simulation FK, step INT, sim_curr_time DateTimeField, persona_movements JSONB; UNIQUE(simulation, step); indexed on (simulation, step)
- Created migration `0009_demo_conversation_tables.py` (name kept to satisfy 0010 dependency)
- Updated migration `0010` to remove AlterField calls for models not yet created (conceptnode, demo, demomovement, keywordstrength, runtimestate, spatialmemory); those will be added when US-006/007 implement those models
- Registered both models in Django admin with list_display and search_fields
- Files changed: `frontend_server/translator/models.py`, `frontend_server/translator/admin.py`, `frontend_server/translator/migrations/0009_demo_conversation_tables.py`, `frontend_server/translator/migrations/0010_alter_conceptnode_id_alter_conversation_id_and_more.py`
- **Learnings for future iterations:**
  - Migration 0009 name must be `0009_demo_conversation_tables` to match 0010 dependency — keep this name for future model additions to 0009 or create 0009b, 0009c migrations
  - Migration 0010 was trimmed to only reference existing models; US-006/007 should create new migrations (0011+) for their AlterField needs
  - Use `migrations.AddConstraint` with `UniqueConstraint` and `migrations.AddIndex` separately for uniqueness + explicit index on same fields
---

## 2026-03-12 - US-006
- Created `ConceptNode` model with all required fields (persona FK, node_id, node_count, type_count, node_type, depth, created, expiration, last_accessed, subject, predicate, object, description, embedding_key, poignancy, keywords JSONB, filling JSONB)
- UNIQUE constraint on (persona, node_id) via `constraints = [UniqueConstraint(...)]`
- Indexes on (persona, node_type), (persona, subject), (persona, -created DESC) via Meta.indexes
- Created `KeywordStrength` model with persona FK, keyword, strength_type ('event'/'thought'), strength (INT default 0)
- UNIQUE constraint on (persona, keyword, strength_type) via `constraints = [UniqueConstraint(...)]`
- Created migration `0011_conceptnode_keywordstrength.py` with CreateModel + AddConstraint + AddIndex ops
- Registered both models in admin.py with list_display, list_filter, search_fields
- Files changed: `frontend_server/translator/models.py`, `frontend_server/translator/admin.py`, `frontend_server/translator/migrations/0011_conceptnode_keywordstrength.py`
- **Learnings for future iterations:**
  - Use `constraints = [UniqueConstraint(...)]` in Meta (not `unique_together`) when using `AddConstraint` in migration — avoids Django state mismatch
  - Descending index `models.Index(fields=["persona", "-created"])` works in Django 4.2 and renders as DESC in SQL
  - Migration index names must be <= 30 chars for some backends — keep names concise
---

## 2026-03-12 - US-007
- Created `SpatialMemory` model: OneToOne to Persona, `tree` JSONB (default dict)
- Created `RuntimeState` model: key VARCHAR UNIQUE, value JSONB, updated_at auto_now
- Created `Demo` model: name UNIQUE, all simulation-like fields, persona_names JSONB, step, total_steps
- Created `DemoMovement` model: demo FK, step INT, agent_movements JSONB, UNIQUE(demo, step)
- Created `ConversationParticipant` model: FK to Conversation (string ref) and Persona, UNIQUE(conversation, persona)
- Created migration `0012_spatialmemory_runtimestate_demo_demomovement_conversationparticipant.py`
- All models registered in Django admin with list_display and search_fields
- Files changed: `frontend_server/translator/models.py`, `frontend_server/translator/admin.py`, `frontend_server/translator/migrations/0012_*.py`
- **Learnings for future iterations:**
  - `ConversationParticipant` is placed before `Conversation` in models.py — must use string reference `"Conversation"` for the FK to avoid F821 undefined name error
  - ruff-format auto-reformatted the file on first pre-commit run — subsequent run passes clean
---

## 2026-03-12 - US-008
- Created `backend_server/utils/db_utils.py` with `set_runtime_state(key, value)` and `get_runtime_state(key, default=None)` backed by the `RuntimeState` table
- `reverie.py`: replaced `curr_sim_code.json` and `curr_step.json` temp file writes with `set_runtime_state` calls
- `reverie.py`: replaced `path_tester_env.json` file read (with `os.remove`) with `get_runtime_state("path_tester_env")` + `set_runtime_state("path_tester_env", None)` to clear after read
- `reverie.py`: replaced `path_tester_out.json` file write with `set_runtime_state("path_tester_out", s_mem)`
- `frontend_server/translator/views.py`: `path_tester_update` now calls `RuntimeState.objects.update_or_create` instead of writing to `temp_storage/path_tester_env.json`
- `backend_server/constant.py`: removed `fs_temp_storage` constant
- Files changed: `backend_server/constant.py`, `backend_server/reverie.py`, `backend_server/utils/db_utils.py`, `frontend_server/translator/views.py`
- **Learnings for future iterations:**
  - `db_utils.py` follows the same lazy-import pattern as `db_persistence.py` (imports `translator.models` inside the function to allow non-Django usage)
  - `views.py` in the frontend can directly import from `translator.models` since it IS a Django view
  - Clearing a RuntimeState entry: use `set_runtime_state(key, None)` — the value is stored as JSON null, and `get_runtime_state` returns None by default anyway; truthy check `if tester_val:` guards against None
---

## 2026-03-12 - US-009
- Refactored `Scratch.__init__` to accept `Optional[str] = None` for legacy file path and `persona_id: Optional[int] = None` for DB mode
- Added `self._persona_id` to track DB mode; when `persona_id` provided, calls `_load_from_db_row()` instead of file loading
- Added `@classmethod load_from_db(cls, persona_id)` that returns `cls(persona_id=persona_id)`
- Added `_load_from_db_row()`: lazy-imports `translator.models.PersonaScratch`, uses `select_related("persona")`, strips timezone from DateTimeFields, converts JSONB lists to 3-tuples for act_event/act_obj_event
- Added `_save_to_db()`: lazy-imports models, makes naive datetimes timezone-aware via `django.utils.timezone.make_aware`, converts tuples to lists for JSONB, calls `update_or_create` on PersonaScratch and `.update()` on Persona identity fields
- Modified `save(out_json=None)`: when `out_json is None` and `_persona_id` set, calls `_save_to_db()`; otherwise uses legacy file path
- Files changed: `backend_server/persona/memory_structures/scratch.py`
- **Learnings for future iterations:**
  - Django USE_TZ=True returns timezone-aware datetimes from DateTimeField — must strip tzinfo when loading into naive-datetime codebase (`.replace(tzinfo=None)`) and make_aware when saving
  - JSON serialization loses tuple types — act_event/act_obj_event stored as lists in JSONB, need `_to_triple()` helper to restore 3-tuple on load
  - `PersonaScratch.act_address` uses `default=""` (not nullable) — save None as `""`, load `""` as None using `or None` idiom
  - Identity fields (name, first_name, age, innate, etc.) live on the `Persona` row, not on `PersonaScratch` — load via `select_related("persona")`, save via `PersonaModel.objects.filter(pk=...).update(...)`
---

## 2026-03-12 - US-010
- Refactored `AssociativeMemory.__init__` to accept `Optional[str] = None` for legacy file path and `persona_id: Optional[int] = None` for DB mode
- Added `_loading: bool` flag to suppress DB/Qdrant writes during init loading phase — `add_*` methods check `if not self._loading` before persisting
- Added `_load_from_db(persona_id)`: loads embeddings from Qdrant via scroll, then loads ConceptNode rows ordered by node_count, then overrides kw_strength from KeywordStrength table
- Added `_load_embeddings_from_qdrant(persona_id)`: scrolls all Qdrant points for persona_id and builds `self.embeddings` dict in memory
- Added `_save_node_to_db(node)`: upserts ConceptNode row using node_id int (extracted from "node_N" string) with timezone-aware datetimes
- Added `_store_embedding_in_qdrant(embedding_key, vector)`: calls `qdrant_utils.store_embedding()` for DB mode
- Added `_update_keyword_strength_in_db(keywords, predicate, object_str, strength_type)`: uses `get_or_create` + `F()` atomic increment within `transaction.atomic()`
- Added `_embedding_key_to_node: dict[str, ConceptNode]` cache populated in `add_*` methods
- Added `retrieve_similar_nodes(query_vector, top_k)`: uses `qdrant_utils.search_similar()` and resolves embedding_keys via `_embedding_key_to_node` cache
- Modified `save(out_json=None)`: no-op in DB mode (incremental saves), file-based save when out_json provided
- Updated `conftest.py` and `test_execute_converse.py` to set new `_persona_id`, `_simulation_id`, `_loading`, `_embedding_key_to_node` when using `object.__new__()`
- Files changed: `backend_server/persona/memory_structures/associative_memory.py`, `backend_server/conftest.py`, `backend_server/tests/test_execute_converse.py`
- **Learnings for future iterations:**
  - Use `_loading = True` during `__init__` to suppress side effects in methods called during loading — set to False after loading completes
  - `ConceptNode.node_id` in DB is IntegerField storing the N from "node_N"; convert with `int(node.node_id.replace("node_", ""))`
  - When using `object.__new__()` in tests to bypass `__init__`, must set ALL new instance attributes added to `__init__` — or tests will get AttributeError
  - Qdrant scroll for embeddings: `point.vector` can be list or dict — check `isinstance(vec, list)` before using
  - `get_or_create` + `F("strength") + 1` in `transaction.atomic()` is the atomic increment pattern for Django without raw SQL
---

## 2026-03-12 - US-013
- Removed `fs_storage` import from `reverie.py` (also removed `json`, `os`, `shutil`, `copyanything`, `check_if_file_exists`, `get_or_create_simulation`, `save_simulation_step`)
- `__init__`: loads `Simulation` row by `sim_code` via ORM, loads `EnvironmentState` for current step for agent positions, loads all active `Persona` objects via `Persona(persona_id=<id>)` DB mode
- `save()`: writes Simulation `curr_time`, `step`, `status="paused"` via ORM; calls `persona.save()` (no-arg = DB mode) for each persona
- `start_server()`: reads `EnvironmentState` table for current step (instead of env JSON file); writes `MovementRecord` table for movements (instead of movement JSON file); removed `sim_folder` local var
- `open_server()`: removed `sim_folder` local var and `shutil.rmtree` calls; "exit" command just breaks the loop
- Files changed: `backend_server/reverie.py`
- **Learnings for future iterations:**
  - `self._db_sim: Optional[Any]` — always guard `.pk` access with `if self._db_sim is not None:` to satisfy mypy
  - EnvironmentState is written by frontend; backend polls by checking `EnvironmentStateModel.objects.filter(...).first()` and waits if None
  - MovementRecord uses `update_or_create(simulation=..., step=..., defaults={...})` for idempotent step writes
---

## 2026-03-12 - US-012
- Updated `Persona.__init__` to accept `persona_id: Optional[int] = None`; when provided, calls `_init_from_db(persona_id)`
- `_init_from_db`: loads `scratch` via `Scratch.load_from_db(persona_id)`, derives `name` from `scratch.name`, loads `a_mem` via `AssociativeMemory(persona_id=persona_id)`, loads `s_mem` via `MemoryTree(persona_id=persona_id)`
- `save(save_folder=None)` now accepts optional save_folder; when None calls DB-mode save on all three memory structures; when provided uses legacy file paths
- Legacy `Persona(name, folder_mem_saved)` call site in `reverie.py` still works unchanged (positional args compatible)
- Files changed: `backend_server/persona/persona.py`
- **Learnings for future iterations:**
  - `Persona.name` in DB mode is loaded from `scratch.name` (which reads from the Persona DB row via select_related)
  - `Persona.save(None)` vs `Persona.save(folder)` pattern: None = DB mode, string = file mode
---

## 2026-03-12 - US-011
- Refactored `MemoryTree.__init__` to accept `Optional[str] = None` (legacy) and `persona_id: Optional[int] = None` (DB mode)
- Added `_load_from_db(persona_id)`: uses `get_or_create` to load or create SpatialMemory row, sets `self.tree` from JSONB
- Modified `save(out_json=None)`: calls `_save_to_db()` when in DB mode (no out_json); legacy file write when out_json provided
- Added `_save_to_db()`: `update_or_create` on SpatialMemory with current `self.tree`
- All existing tree navigation methods (`get_str_accessible_sectors`, etc.) unchanged — work with DB-loaded data identically
- Files changed: `backend_server/persona/memory_structures/spatial_memory.py`
- **Learnings for future iterations:**
  - `get_or_create` is the right pattern for SpatialMemory since the row may not exist on first use
  - `save(out_json=None)` pattern is consistent with Scratch and AssociativeMemory — no-arg means DB mode
---

## Codebase Patterns
- Project uses mypy for typechecking, ruff for linting — run via pre-commit hooks on commit
- `backend_server/pyproject.toml` configures mypy and ruff for the backend
- `backend_server/requirements.txt` and `frontend_server/requirements.txt` track Python deps
- Django version differs: backend uses 2.2, frontend uses 4.2.20
- Use `pre-commit run --files <file>` to typecheck/lint individual files before committing
- Qdrant point IDs should be deterministic UUIDs (uuid5) derived from (persona_id, embedding_key) to make upserts idempotent
- `_ensure_collection()` pattern: check existing collections set before creating to avoid errors on repeated calls
- Module-level `_client: Optional[QdrantClient] = None` + lazy init in `_get_client()` is the singleton pattern used
- Use `_loading = True` flag in `__init__` to suppress DB/Qdrant writes during loading; set False after loading completes
- When using `object.__new__()` in tests, always set ALL instance attributes introduced by `__init__` to avoid AttributeError

## 2026-03-12 - US-001
- Added `qdrant/qdrant` service to `docker-compose.yml` with port 6333, healthcheck, and `qdrant-data` named volume
- Added `qdrant-client>=1.9.0` to `backend_server/requirements.txt`
- Added `qdrant-client>=1.9.0` to `frontend_server/requirements.txt`
- Files changed: `docker-compose.yml`, `backend_server/requirements.txt`, `frontend_server/requirements.txt`
- **Learnings for future iterations:**
  - Pre-commit hooks run mypy/ruff on commit; only Python files trigger those checks
  - `docker-compose.yml` healthcheck for Qdrant uses `/healthz` endpoint
  - Both backend and frontend have separate requirements.txt — both need qdrant-client
---

## 2026-03-12 - US-002
- Created `backend_server/utils/qdrant_utils.py` with 5 public functions:
  - `store_embedding(persona_id, simulation_id, embedding_key, vector)` — upserts via deterministic UUID5 point ID
  - `get_embedding(persona_id, embedding_key)` — scroll-based payload filter retrieval
  - `search_similar(persona_id, query_vector, top_k=20)` — cosine ANN search with persona_id filter
  - `delete_persona_embeddings(persona_id)` — bulk delete by payload filter
  - `copy_persona_embeddings(src_persona_id, dst_persona_id, dst_simulation_id)` — paginated scroll+upsert deep-copy
- Collection `persona_embeddings` auto-created on first use with EMBEDDING_DIMENSION (default 1536) and cosine distance
- Qdrant client is module-level singleton via `_get_client()` lazy initializer
- Files changed: `backend_server/utils/qdrant_utils.py`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - `qdrant_client.scroll()` returns `(results, next_offset)` tuple — use next_offset for pagination, break when None
  - Point vectors returned by scroll() can be `list | dict | None` — check `isinstance(vec, list)` before returning
  - `models.FilterSelector(filter=...)` is required for `client.delete()` when filtering by payload
  - mypy ignores missing imports for qdrant_client (configured in pyproject.toml) — no stubs needed
---
