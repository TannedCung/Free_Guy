# Ralph Progress Log
Started: Tue Mar 17 03:54:12 PM +07 2026
---

## Codebase Patterns
- Settings split into base.py + development.py + production.py; use `from .base import *` pattern
- Run `python3 manage.py` (not `python`) on this system
- Install packages with `--break-system-packages` flag since no venv is active
- `DJANGO_SECRET_KEY` env var is required; use `DJANGO_ENV=development` for dev settings
- Pre-commit hooks run ruff + mypy + ESLint; all must pass before commit

## 2026-03-17 - US-001
- Added `djangorestframework-simplejwt>=5.3.0` to frontend_server/requirements.txt
- Added `rest_framework_simplejwt` and `rest_framework_simplejwt.token_blacklist` to INSTALLED_APPS in base.py
- Added `REST_FRAMEWORK` setting with JWTAuthentication as default auth class
- Added `SIMPLE_JWT` config: 15min access token, 30day refresh, rotate+blacklist enabled
- Migrations applied cleanly (token_blacklist tables created)
- Files changed: frontend_server/requirements.txt, frontend_server/frontend_server/settings/base.py
- **Learnings for future iterations:**
  - The `from datetime import timedelta` import goes at the bottom of base.py (after all other imports); ruff was fine with it
  - Token blacklist migration applies automatically via `python3 manage.py migrate`
  - Django system check (`python3 manage.py check`) is a good typecheck proxy for backend
---

## 2026-03-17 - US-002
- Map model was already implemented in models.py with all required fields (id CharField PK, name, description, preview_image_url blank, maze_name, max_agents default 25, is_active default True)
- MapAdmin registered in admin.py with list_display=[id, name, maze_name, max_agents, is_active]
- Migration 0014_add_map_model.py creates the Map table
- Migration 0015_seed_the_ville_map.py data migration seeds 'the_ville' entry
- All migrations applied cleanly (verified via showmigrations)
- Files changed: frontend_server/translator/models.py, frontend_server/translator/admin.py, frontend_server/translator/migrations/0014_add_map_model.py, frontend_server/translator/migrations/0015_seed_the_ville_map.py
- **Learnings for future iterations:**
  - When checking if a story is done, verify git status: untracked migration files mean work was done but not committed yet
  - Data migrations use `apps.get_model()` pattern and `get_or_create()` for idempotency
  - Pre-commit hooks stash unstaged files before running checks, then restore them after
---

## 2026-03-17 - US-003
- Added `owner` ForeignKey to User (null=True, on_delete=SET_NULL) to Simulation model
- Added `map_id` ForeignKey to Map (null=True, on_delete=SET_NULL) to Simulation model
- Added `visibility` CharField with Visibility choices (private/public/shared), default private
- Added `get_user_model()` import pattern to models.py
- Updated SimulationAdmin list_display to include owner, map_id, visibility
- Migration 0016_add_simulation_owner_visibility.py generated and applied cleanly
- Files changed: frontend_server/translator/models.py, frontend_server/translator/admin.py, frontend_server/translator/migrations/0016_add_simulation_owner_visibility.py
- **Learnings for future iterations:**
  - Use `get_user_model()` and store result as `User = get_user_model()` at module level; don't import User directly from auth
  - ForeignKey to other models in same app can use string reference `"Map"` (forward reference) or class directly
---
